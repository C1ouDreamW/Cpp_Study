//在布置某活动现场时，某负责人发现有一个长为 l块地板的直线区域有电线束裸露。为了美观并且节约成本，他找来 n块长度不一的废弃地毯，由于头脑不太清醒，他将这 n块地毯随意地铺设在了上面，导致某些地板上有多层地毯堆叠。现在，给你n组数据，包括每块地毯铺设的起点，请你找出直线区域内的第几块地板上堆叠了最多层的地毯并说出地毯层数。
//输入第一行包含一个整数l表示电线束裸露的直线区域的地板块数。
//第二行包含一个整数n表示地毯的块数。
//接下来包含n 行，每一行有两个整数，分别代表第i 块地毯的长度和铺设起点。
//输出共一行，包含两个整数，分别为堆叠了最多层地毯的地板的序号和该块地板的地毯层数。若题目中有多个答案，仅需输出符合要求的地板中的最小序号。
#include <stdio.h>
#include <stdlib.h>
#include <string.h>





